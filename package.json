{
  "name": "fable-ous",
  "version": "0.2.9",
  "description": "A minimal reversible presentation preset for native Codex.",
  "author": "Lars",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lars-hv/fable-ous.git"
  },
  "homepage": "https://github.com/lars-hv/fable-ous#readme",
  "type": "module",
  "files": [
    ".agents/",
    ".claude-plugin/",
    "bin/",
    "docs/",
    "evals/public/",
    "plugins/",
    "scripts/",
    "src/",
    "LICENSE",
    "README.md"
  ],
  "bin": {
    "fable-ous": "bin/fable-ous.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "node --check bin/fable-ous.mjs && node --check src/cli.mjs && node --check scripts/check-package.mjs && node --check scripts/validate-plugins.mjs && node --check plugins/fable-ous/scripts/activation.mjs && node --test test/*.test.mjs",
    "validate:plugins": "node scripts/validate-plugins.mjs",
    "check:package": "node scripts/check-package.mjs",
    "prepublishOnly": "npm run check && npm run validate:plugins && npm run check:package"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "codex",
    "claude-code",
    "communication",
    "product-strategy",
    "output-style"
  ],
  "license": "MIT"
}
